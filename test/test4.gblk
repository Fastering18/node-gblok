-- CLOSURES

fungsi b()
     lokal e = 10

     fungsi c() 
          fungsi d() print(e) tutup
          kembali d
     tutup
     kembali c
tutup

--[[
  lokal Lexer = (proses->binding)("../src/lexer.js")
lokal res = (Lexer->Lexer)("sos", "print")
print(res)
]]

lokal res = (proses->binding)("../regexp")
print(res)